{
  "name": "line-task-bot",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "build:frontend": "npm --prefix frontend ci && npm --prefix frontend run build",
    "render-build": "npm ci && npm run build:frontend"
  },
  "dependencies": {
    "cookie": "^0.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^3.0.3",
    "node-fetch": "^3.3.2"
  }
}
